apiVersion: v1
kind: Service
metadata:
  name: hello-nginx-medium
  labels:
    run: hello-nginx-medium
    gefyra-loadtest: "true"
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: hello-nginx-medium
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-nginxdemo-medium
  labels:
    gefyra-loadtest: "true"
spec:
  selector:
    matchLabels:
      app: hello-nginx-medium
  replicas: 2
  template:
    metadata:
      labels:
        app: hello-nginx-medium
    spec:
      containers:
      - name: hello-nginx
        image: nginxdemos/hello
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "100m"
            memory: "64Mi"
          limits:
            cpu: "250m"
            memory: "128Mi"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-nginx-medium
  namespace: default
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello-nginx-medium
            port:
              number: 80
