apiVersion: v1
kind: Service
metadata:
  name: hello-nginx-light
  labels:
    run: hello-nginx-light
    gefyra-loadtest: "true"
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: hello-nginx-light
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-nginxdemo-light
  labels:
    gefyra-loadtest: "true"
spec:
  selector:
    matchLabels:
      app: hello-nginx-light
  replicas: 1
  template:
    metadata:
      labels:
        app: hello-nginx-light
    spec:
      containers:
      - name: hello-nginx
        image: nginxdemos/hello
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "50m"
            memory: "32Mi"
          limits:
            cpu: "100m"
            memory: "64Mi"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-nginx-light
  namespace: default
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello-nginx-light
            port:
              number: 80
